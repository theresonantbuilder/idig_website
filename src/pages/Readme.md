In powershell
npm run build


To push to github
git add .
git commit -m "Description"
git push

*The iDIG website is built on Vite and function hosted on Vercel. www.i-dig.io DNS is through vercel in Godaddy. 